[PARSER]
    Name         nginx_access
    Format       json
    Time_Key     time
    Time_Format  %Y-%m-%dT%H:%M:%S%z

[PARSER]
    Name   nginx_error
    Format regex
    Regex ^(?<time_local>[^\]]+)\s\[(?<log_level>[^\]]+)\]\s(?<message>.*)